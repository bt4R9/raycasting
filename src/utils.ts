export const toRadian = (degree: number) => {
  return degree * Math.PI / 180;
}

export const to_radian = (degree: number) => {
  return degree * Math.PI / 180;
}

export const to_degree = (radian: number) => {
  return radian * 180 / Math.PI;
}
